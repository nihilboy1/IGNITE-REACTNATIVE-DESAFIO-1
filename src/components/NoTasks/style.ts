import { StyleSheet } from 'react-native'

export const S = StyleSheet.create({
  container: {
    marginTop: 65,
    width: '100%',
    alignItems: 'center'
  },
  text: {
    fontSize: 18,
    color: "#808080",
  },
  textBold: {
    marginTop: 20,
    fontWeight: "bold",
    fontSize: 18,
    color: "#808080",
  }
})
